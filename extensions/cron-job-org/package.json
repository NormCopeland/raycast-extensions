{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "cron-job-org",
  "title": "Cron-Job.org",
  "description": "Manage your cron jobs from cron-job.org",
  "icon": "extension-icon.png",
  "author": "NormC",
  "platforms": [
    "macOS",
    "Windows"
  ],
  "categories": [
    "Data",
    "Developer Tools"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "list-jobs",
      "title": "List Jobs",
      "subtitle": "cron-job.org",
      "description": "List all your cron jobs from cron-job.org",
      "mode": "view"
    },
    {
      "name": "create-job",
      "title": "Create Job",
      "subtitle": "cron-job.org",
      "description": "Create a new cron job on cron-job.org",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.3",
    "@raycast/utils": "^1.17.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  },
  "preferences": [
    {
      "name": "apiKey",
      "type": "password",
      "title": "API Key",
      "required": true,
      "description": "cron-job.org API key (Console \u2192 Settings). Sent as Authorization: Bearer <key>"
    }
  ]
}
